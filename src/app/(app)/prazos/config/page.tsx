import { ConfigView } from "@/features/prazos/components/config/config-view";

export const metadata = { title: "Configurações · Prazos · jus·assessoria" };

export default function ConfigPage() {
  return <ConfigView />;
}
