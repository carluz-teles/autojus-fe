import { FilaView } from "@/features/prazos/components/fila/fila-view";

export const metadata = { title: "Fila · Prazos · jus·assessoria" };

export default function FilaPage() {
  return <FilaView titulo="Fila" />;
}
