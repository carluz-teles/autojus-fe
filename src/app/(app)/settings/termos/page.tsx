import { TermsPanel } from "@/features/integrations/components/terms-panel";

// Seção Termos: o que monitorar (OABs hoje; CNPJs e nomes de parte no roadmap).
// Separada de Integrações — lá ficam as conexões com as fontes.
export default function SettingsTermosPage() {
  return <TermsPanel />;
}
