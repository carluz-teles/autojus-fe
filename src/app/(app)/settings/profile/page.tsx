import { ProfilePanel } from "@/features/profile/components/profile-panel";

// Seção Perfil: dados pessoais + segurança da conta (headless, via useUser).
export default function SettingsProfilePage() {
  return <ProfilePanel />;
}
