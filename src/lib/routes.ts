/** Default authenticated destination; the domain root is the public landing page. */
export const APP_HOME_PATH = "/notificacoes";

/** Novo usuário cai direto no wizard (sem flash de APP_HOME antes do gate redirecionar). */
export const ONBOARDING_PATH = "/onboarding";
